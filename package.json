{
  "name": "myblueprint-career-launch",
  "version": "1.0.0",
  "description": "myBlueprint Career Launch Landing Page - December 2nd Virtual Career Fair",
  "main": "server.js",
  "scripts": {
    "dev": "node server.js",
    "start": "node server.js",
    "static": "python3 -m http.server 8080 || python -m http.server 8080",
    "build": "mkdir -p public && cp -r *.html styles js images fonts public/ 2>/dev/null || cp *.html public/ && [ -d styles ] && cp -r styles public/ || true && [ -d js ] && cp -r js public/ || true && [ -d images ] && cp -r images public/ || true && [ -d fonts ] && cp -r fonts public/ || true",
    "test": "echo 'Testing form validation and responsive design'",
    "validate-html": "echo 'HTML validation would run here'",
    "validate-css": "echo 'CSS validation would run here'",
    "check-a11y": "echo 'Accessibility audit would run here'",
    "optimize": "echo 'Image optimization and minification would run here'"
  },
  "keywords": [
    "myblueprint",
    "career-fair",
    "education",
    "landing-page",
    "responsive",
    "accessibility"
  ],
  "author": "myBlueprint",
  "license": "UNLICENSED",
  "devDependencies": {
    "nodemon": "^3.0.1"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1"
  },
  "browserslist": [
    "> 1%",
    "last 2 versions",
    "not dead",
    "not ie <= 11"
  ],
  "repository": {
    "type": "git",
    "url": "private"
  },
  "engines": {
    "node": ">=14.0.0"
  }
}